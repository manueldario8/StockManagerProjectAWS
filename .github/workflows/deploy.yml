name: Deploy API

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Clonar repo
        uses: actions/checkout@v3

      - name: Instalar .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: 10.0

      - name: Restaurar dependencias
        run: dotnet restore ./StockManager.API/StockManager.sln

      - name: Build
        run: dotnet build ./StockManager.API/StockManager.sln --configuration Release --no-restore

      - name: Publish
        run: dotnet publish ./StockManager.API/StockManager.API.csproj -c Release -o publish

      - name: Copiar al servidor (SCP)
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          source: "publish/*"
          target: "/var/www/stockmanagerapi"
